spring:
  profiles:
    active:
    on_profile: local
  datasource:
    url:  jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: Dmitry
    password: Sport-life--h2

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db-changelog-master.xml

server:
  port: 49182
  servlet:
    context-path: /api
  error:
    include-message: always
