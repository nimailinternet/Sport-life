spring:
  profile:
    active:
      on_profile: docker
  datasource:
    url: jdbc:postgresql://postgres:5432/postgres
    driver-class-name: org.posgresql.Driver
    username: Dmitry
    password: Sport-life--postgreSQL
  jpa:
    hibernate:
      ddl-auto: none
    snow-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQlDialect
  liquibase:
    enabled: true
    change-log: classpath:db/db/changelog/db-changelog-master.xml
server:
  port: 49182
  servlet:
    context_path: /api
  error:
    include-message: always

